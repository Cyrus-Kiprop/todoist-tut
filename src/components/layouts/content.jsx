import React from "react";
import { Sidebar } from "./Sidebar";

//the content component holds all other components in place
export const Content = () => (
  <section>
    <Sidebar />
  </section>
);
